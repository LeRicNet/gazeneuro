% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attention_analysis.R, R/uncertainty.R
\name{detect_fixations}
\alias{detect_fixations}
\title{Attention Analysis Functions for gazeneuro (Base R version)}
\usage{
detect_fixations(gaze_data, params = default_uncertainty_params())

detect_fixations(gaze_data, params = default_uncertainty_params())
}
\arguments{
\item{gaze_data}{Data frame with velocity_deg_s column}

\item{params}{Parameters from default_uncertainty_params()}

\item{integrated_data}{Data frame from integrate_all_gaze_points()}

\item{velocity_threshold}{Velocity threshold in normalized units/second (default: 0.1)}

\item{min_fixation_duration}{Minimum fixation duration in seconds (default: 0.1)}

\item{screen_diagonal_deg}{Visual angle of screen diagonal in degrees (default: 30)}
}
\value{
Data frame with added columns for velocity, is_fixation, fixation_id, fixation_duration

Data frame with fixation labels
}
\description{
Functions for generating attention heatmaps and analyzing gaze patterns.
Includes fixation detection, 2D slice heatmaps, and 3D volumetric attention maps.
This version uses only base R functions for maximum compatibility.
Detect fixations using velocity threshold

Identifies fixation periods in gaze data using a velocity-based threshold.
Samples below the threshold are classified as fixations; above as saccades.

Identifies fixation periods where gaze velocity falls below threshold.
Also groups consecutive fixation samples into fixation events.
}
